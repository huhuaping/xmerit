# get the plus and minus symbols

get the plus and minus symbols

## Usage

``` r
get.sign(df)
```

## Arguments

- df:

  numeric

## Value

out

## Examples

``` r
set.seed(123)
v <- rnorm(10)
get.sign(v)
#>  [1] "-" "-" "+" "+" "+" "+" "+" "-" "-" "-"

```
